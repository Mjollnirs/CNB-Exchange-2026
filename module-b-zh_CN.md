# 模块 B：ReClaim REST API 客户端前端

- **竞赛时长：**3 小时
- **应用类型：**面向旅客的单页应用

## 简介

上海浦东国际机场（PVG）运营统一的全机场失物招领服务 ReClaim。旅客使用 ReClaim 报告遗失物品、跟踪申领进度、在其他地方找回物品后撤回申领，并维护自己的联系信息。

本模块要求构建 ReClaim 旅客门户，即所提供 ReClaim REST API 的用户端前端。你只负责客户端用户界面和用户体验；工作人员桌面和管理界面不在本模块范围内。

## 项目与任务概述

你将获得可运行的 Module A ReClaim API 解决方案。必须使用该方案，不得自行实现、替换或修改后端。

随解决方案提供的 API 文档是以下内容的唯一权威依据：

- 请求字段。
- 响应结构。
- 验证规则。
- HTTP 状态码。
- 业务规则。

### 评分辅助延迟

所提供 API 中的以下请求会被有意延迟约 2 秒：

```http
GET /passenger/claims?status=rejected
```

其他状态筛选正常响应。此延迟仅用于可靠测试响应乱序。

客户端不能只对 `rejected` 做特殊处理，而应在每次筛选变化时统一防止过期响应覆盖当前视图。

### 技术要求

- 使用现代 JavaScript 框架创建单页应用（SPA）。
- 允许使用其他库。
- 路由必须由框架管理。
- 重新加载某个路由时应恢复同一页面，但未保存的表单输入和临时消息除外。

API 基础 URL：

```text
https://module-a-solution-wsYY-YYYY.foredu.cn/api
```

本文中的所有 API 路径均相对于该基础 URL。

后端 OpenAPI 文档位于：

```text
assets/api-docs/
```

在浏览器中打开：

```text
assets/api-docs/index.html
```

同时提供可运行的后端副本：

1. 解压 `assets/solution-api.zip`。
2. 将 `assets/database/reclaim-db.sql` 导入 MySQL。
3. 在 `api/.env` 中配置数据库凭据。
4. 按压缩包内 `RUN.md` 的说明启动服务。

该本地后端与托管 API 行为一致，包括对 `rejected` 筛选的有意延迟。

额外提供辅助端点：

```http
POST /reset-db
```

可在开发或测试过程中按需将数据库恢复为标准种子数据。该端点不属于需要构建的旅客门户界面。

## 允许使用的 API

| 范围 | API 操作 |
|---|---|
| 旅客认证 | `POST /passenger/register`、`POST /passenger/login`、`POST /passenger/logout` |
| 旅客申领 | `GET /passenger/claims`、`GET /passenger/claims/{id}`、`POST /passenger/claims`、`POST /passenger/claims/{id}/withdraw` |
| 旅客资料 | `GET /passenger/profile`、`PUT /passenger/profile` |
| 公共跟踪 | `GET /claims/track/{referenceCode}` |
| 航站楼选择 | `GET /terminals` |

不得调用或创建以下功能的界面：

- 工作人员认证。
- 航站楼管理。
- 捡拾物品管理。
- 工作人员申领处理。
- 匹配或结案。
- 仪表板。
- `GET /my-terminals`。

`GET /terminals` 只能作为新申领表单中航站楼选择器的只读数据源。

## 测试账户

| 邮箱 | 密码 | 说明 |
|---|---|---|
| `passenger1@email.com` | `passenger123` | 有效旅客 |
| `passenger2@email.com` | `passenger123` | 有效旅客 |
| `passenger31@email.com` | `passenger123` | 已停用账户，登录必须失败 |

## 身份认证

门户必须使用旅客认证系统。已认证请求必须携带：

```http
Authorization: Bearer {token}
```

要求：

- 页面重新加载后会话必须继续有效。
- 退出登录时必须清除当前会话。
- 需要认证的请求收到 `401 Unauthenticated` 时必须清除当前会话。
- 只能使用旅客凭据和旅客令牌。
- 不得使用工作人员或具有更高权限的系统操作员凭据和令牌。

## 应用外壳与导航

应用必须具有清晰、一致且面向旅客的布局与导航。视觉精致度不是最重要的因素；关键是应用能够运行、易于使用，并正确支持规定的旅客工作流。

如需图标，可使用：

```text
assets/fontawesome
```

### 路由要求

- 使用基于路径的客户端路由，即框架 History 模式。
- 评分人员可能直接打开规定的路径。
- 不得使用哈希路由，例如 `#/claims`。
- 未认证访客必须能够注册、登录和使用公共申领跟踪。
- 已认证旅客还必须能够提交申领、查看自己的申领、打开申领详情、编辑个人资料和退出登录。
- 认证路由必须受保护。
- 未认证访客请求认证路由时，应跳转至登录页。
- 登录成功后，应返回原本请求的 URL。
- 默认路由 `/`：
  - 已认证旅客跳转到“我的申领”页。
  - 其他用户跳转到登录页。
- 界面中不得出现工作人员或管理功能的控件与导航。

### 界面状态

界面必须通过视觉和文字明确区分以下状态：

- **加载中：**正在获取数据或执行操作。可使用加载图标、骨架屏或 `Loading...` 消息。等待时不得显示空列表或成功消息。
- **空状态：**请求成功但没有内容，例如 `You have no claims yet`。该状态不得看起来像错误或加载中。
- **成功：**操作正确完成，例如提交申领或更新资料后的确认。
- **错误：**请求失败或验证阻止提交。显示清晰消息，并在适用时提供重试方式。

## 注册页

必需路由：

```text
/register
```

注册页必须能够创建旅客账户。

表单字段：

- 名字。
- 姓氏。
- 电子邮箱。
- 电话号码，可选。
- 地址第 1 行。
- 地址第 2 行，可选。
- 城市。
- 邮编。
- 国家。
- 密码，至少 8 个字符。

提交前必须验证：

- 必填字段。
- 邮箱格式。
- 密码长度。

必须显示 API 返回的字段级验证消息。

注册成功后：

1. API 返回旅客令牌和旅客详情。
2. 保存会话。
3. 跳转到“我的申领”页。

## 登录与退出

登录路由：

```text
/login
```

登录页接收邮箱地址和密码，并使用正确的旅客登录端点认证。

登录成功后：

- 保存返回的旅客令牌和旅客详情。
- 如果用户原本请求了认证路由，继续访问该 URL。
- 如果没有原请求，进入“我的申领”页。

凭据无效或账户停用时返回 `401`。界面应显示清晰的认证错误，但不得暴露敏感信息。

已认证旅客必须能够从应用外壳退出登录。

退出登录时：

- 清除本地会话。
- 跳转到登录页。
- 即使远程令牌已经失效，也必须清除本地会话。

## 新申领页

必需路由：

```text
/claims/new
```

已认证旅客必须能够在此报告遗失物品。

### 多步骤流程

1. **地点与时间**
   - 选择航站楼。
   - 选择遗失日期。
2. **物品详情**
   - 输入类别。
   - 输入品牌。
   - 输入颜色。
   - 输入描述。
   - 输入航班号。
3. **检查并提交**
   - 显示完整摘要。
   - 允许返回任一前序步骤。
   - 返回前序步骤时不得丢失数据。

流程必须显示当前步骤和已完成步骤。只有当前步骤有效时才能继续。

浏览器前进和后退不得：

- 意外提交表单。
- 丢失已输入的值。

### 表单字段

| 字段 | 要求 |
|---|---|
| 航站楼 | 必填 |
| 类别 | 必填，必须使用规定的英文枚举值 |
| 品牌 | 可选 |
| 颜色 | 可选 |
| 描述 | 必填 |
| 遗失日期 | 必填，以 `lost_on` 提交，不得是未来日期 |
| 航班号 | 可选 |

类别必须使用：

`electronics`、`documents`、`luggage`、`clothing`、`jewellery`、`keys`、`other`

客户端验证必须阻止明显无效的提交。API 验证错误应尽可能显示在相关字段旁。

### 草稿恢复

未完成的申领草稿必须在以下情况可恢复：

- 页面重新加载。
- 浏览器关闭后重新打开。

发现草稿时应提供：

- 继续草稿。
- 丢弃草稿。

以下情况不得恢复旧草稿：

- 草稿已经提交。
- 草稿已经丢弃。
- 当前登录的是其他旅客。

### 航站楼加载

- 加载航站楼时显示加载状态。
- 未选择航站楼不得继续。
- 加载失败时显示错误和重试操作。
- 必须从成功的航站楼响应中选择一个航站楼后才能提交申领。

## 客户端申领质量评分

完全在浏览器中计算 `0` 至 `100` 的申领质量分。

该功能仅用于客户端引导：

- 不得调用 API。
- 不得随申领发送。
- 不得表示为找回物品的概率。

相关表单值每次变化时立即重新计算。

### 评分规则

| 规则 | 分值 |
|---|---:|
| 已选择航站楼 | 10 |
| 已选择类别 | 10 |
| 品牌包含非空白内容 | 15 |
| 颜色包含非空白内容 | 10 |
| 航班号包含非空白内容 | 10 |
| 去除首尾空白后的描述至少 40 个字符 | 10 |
| 去除首尾空白后的描述至少 80 个字符 | 额外 10 |
| 描述至少包含 12 个不同单词，不区分大小写并忽略标点 | 10 |
| `lost_on` 为今天或 1 天前 | 15 |
| `lost_on` 为 2 至 3 天前 | 10 |
| `lost_on` 为 4 至 7 天前 | 5 |
| `lost_on` 为 8 天以上、缺失、无效或未来日期 | 0 |

要求：

- 日期规则只能有一项计分。
- 字符阈值可以累计。
- 至少 80 个字符的描述获得 20 分描述长度分。
- 最终结果限制在 `0` 至 `100`。

### 分数标签

| 分数 | 标签 |
|---:|---|
| 0 至 39 | `Needs more detail` |
| 40 至 69 | `Basic` |
| 70 至 89 | `Detailed` |
| 90 至 100 | `Excellent` |

检查步骤必须：

- 显示分项得分。
- 指出哪些可选信息仍可提高分数。

示例：已填写航站楼、类别、品牌和颜色，没有航班号，描述为 85 个字符且至少包含 12 个不同单词，日期为今天，则得分为 `90`。

该分数仅供参考。只要 API 必填字段有效，绝不能阻止提交。

继续或丢弃草稿后，分数必须重新与表单值保持一致。

## 创建申领成功后的确认

使用 API 响应中的新申领详情显示确认模态框。

至少显示：

- 参考编号 `CL-XXXXXXXX`。
- 初始状态 `submitted`。
- 类别。
- 航站楼。
- 遗失日期。

参考编号必须醒目且易于复制。

模态框必须恰好提供两个操作：

1. **Create a new claim**
   - 关闭模态框。
   - 将多步骤表单重置到第一步。
   - 让旅客可以立即提交另一份申领。
2. **Show my claims**
   - 跳转到“我的申领”页。

如果旅客使用以下其他方式关闭模态框，其行为必须等同于 **Show my claims**：

- Escape。
- 关闭按钮。
- 点击背景。

不得让旅客返回已完成且不再显示确认信息的表单。

## 我的申领页

必需路由：

```text
/claims
```

该页面只能获取当前已认证旅客自己的申领。

要求：

- 按 API 返回顺序显示，最新申领在前。
- 使用响应中的 `links` 或 `meta` 支持分页。
- 不得假设所有申领都在同一页。
- 每条申领至少显示：
  - 参考编号。
  - 类别。
  - 航站楼。
  - 遗失日期。
  - 当前状态。
- 支持按状态筛选。
- 每条结果必须链接到相应申领详情页。

### URL 驱动的工作区状态

将所选状态和页码存储在 URL 查询字符串中，例如：

```text
?status=submitted&page=2
```

以下操作必须恢复相同视图：

- 重新加载页面。
- 分享 URL 后打开。
- 浏览器后退。
- 浏览器前进。

改变筛选状态时必须回到第 1 页。

### 防止过期响应覆盖

旅客快速切换筛选条件时，请求可能以不同顺序完成。

应优先取消过期请求；如果无法取消，也必须保证旧响应永远不能覆盖最新选择的视图。

验证步骤：

1. 选择 `rejected`。
2. 在延迟响应返回前快速切换到其他状态，例如 `resolved`。
3. 等待两个请求都结束。
4. 列表和 URL 必须仍显示最后选择的状态，而不是 `rejected`。

分页切换时：

- 保留现有结果。
- 显示非阻塞加载提示。
- 不得短暂显示错误的空状态。

## 申领详情与撤回

必需路由：

```text
/claims/:id
```

获取申领并展示旅客申领响应中的所有有用字段，包括：

- 参考编号。
- 航站楼。
- 类别。
- 品牌和颜色，如有。
- 描述。
- 遗失日期。
- 航班号，如有。
- 状态。
- 创建日期。
- 更新日期。
- 结案日期，如有。
- 匹配物品信息，如 API 返回。

### 撤回规则

旅客只能在本人申领处于以下状态时撤回：

- `submitted`
- `under-review`

要求：

- 只有当前状态允许时才显示撤回操作。
- 发送请求前要求确认。
- 成功后将页面中的申领更新为 API 返回的 `withdrawn` 状态。

## 实时申领旅程

将申领当前状态展示为对旅客友好的流程，而不是只显示原始状态值。

必须正确表示以下 API 状态：

- `submitted`
- `under-review`
- `matched`
- `resolved`
- `rejected`
- `withdrawn`

`rejected` 和 `withdrawn` 是替代结局，不能展示为成功完成的旅程。

## 申领刷新

已认证申领详情页打开时，每 15 秒刷新一次。

要求：

- 上一次刷新仍在进行时不得启动新刷新。
- 页面不可见时暂停自动刷新。
- 页面重新可见时立即刷新。
- 状态变化时无需整页重新加载即可更新旅程和申领信息。
- 状态变化必须通过可见提示和无障碍实时区域进行宣布。
- 申领达到以下状态时停止轮询：
  - `resolved`
  - `rejected`
  - `withdrawn`
- 提供手动刷新控件。
- 手动刷新使用与自动刷新相同的加载和错误处理。

临时刷新失败时：

- 保留上一次成功获取的申领。
- 显示非破坏性警告。
- 不得用空状态或整页错误替换申领。
- 允许通过手动刷新重试。

## 公共申领跟踪

入口路由：

```text
/track
```

结果路由：

```text
/track/:referenceCode
```

任何人都必须能够输入参考编号，在无需登录的情况下跟踪申领。

要求：

- 接受 `CL-XXXXXXXX` 格式的参考编号。
- 成功后跳转到结果路由。
- 只显示公共响应数据：
  - 参考编号。
  - 状态。
  - 类别。
  - 航站楼。
  - 遗失日期。
  - 创建日期。
  - 结案日期，如有。
- 不得尝试获取或显示旅客个人数据。
- `404` 响应必须显示清晰的 `claim not found` 结果。
- 未找到后允许再次搜索。

参考编号必须出现在结果路由中，以便：

- 添加书签。
- 分享链接。
- 重新加载 URL 后再次执行公共查询。

发送请求前：

- 将用户输入转为大写。
- 去除意外的首尾空白。
- 不得改变内部字符。

公共跟踪使用与已认证详情页相同的：

- 实时旅程。
- 15 秒刷新行为。
- 轮询停止规则。

公共跟踪绝不能：

- 调用已认证旅客端点。
- 显示公共响应以外的字段。

## 旅客资料页

必需路由：

```text
/profile
```

页面必须获取当前已认证旅客的资料并允许更新。

显示并允许编辑：

- 名字。
- 姓氏。
- 邮箱地址。
- 电话号码。
- 地址第 1 行。
- 地址第 2 行。
- 城市。
- 邮编。
- 国家。

旅客可以设置至少 8 个字符的新密码。

密码要求：

- 不得显示现有密码。
- 不得发送未更改的密码字段。
- 不得发送空密码字段。

更新端点支持部分更新。

验证错误必须显示在相关字段旁，包括邮箱地址不唯一。

更新成功后：

- 显示 API 返回的当前资料。
- 显示可见的成功确认。

## 错误处理

整个应用至少要处理以下情况：

| 状态或情况 | 必需行为 |
|---|---|
| `401 Unauthenticated` 或凭据无效 | 受保护请求应清除无效会话并要求登录；登录表单应显示认证错误 |
| `404 Not Found` | 说明申领或资源未找到，或当前旅客无权访问 |
| `422 Validation failed` | 显示通用消息和字段级错误 |
| `422` 业务规则失败 | 在失败操作附近显示 API 的描述性消息 |
| 网络错误或 API 不可用 | 说明无法连接服务并允许重试 |

请求进行中时必须防止重复提交，不得静默丢弃 API 错误。

### 页面生命周期与清理

路由或已认证旅客发生变化后，应用必须防止过期响应更新页面。

以下操作不得遗留会作用于错误页面的计时器或未完成请求：

- 自动刷新。
- 手动刷新。
- 路由变化。
- 退出登录。

离开页面时必须清理：

- 计时器。
- 未完成请求。
- 可能更新旧页面的回调。

## 设计、响应式与无障碍

成品的外观和行为必须像公共机场旅客服务，而不是内部管理仪表板。

要求：

- 使用清晰的视觉层级。
- 使用易读的状态指示。
- 状态不能只通过颜色传达。
- 表单必须支持键盘导航。
- 标签必须与控件正确关联。
- 错误消息必须与相应控件正确关联。
- 交互控件必须具有可见焦点状态。
- 交互控件必须使用有意义的文本。
- 对撤回申领等破坏性或后果重大的操作进行确认。
- 在常见移动端和桌面端视口宽度下，无需横向滚动即可使用。
- 整个门户一致地使用适当的日期、加载和反馈呈现方式。

## 评分

Module B 使用所提供的最新稳定版 Google Chrome 进行评分。

评分内容包括：

- 正确集成所提供的 Module A API。
- 旅客工作流的完整性和正确性。
- SPA 路由。
- 认证持久化。
- 路由保护。
- 多步骤表单状态。
- 按旅客隔离的草稿恢复。
- 表单验证。
- 正确的客户端申领质量计算、分项得分和指导。
- URL 驱动的分页和筛选。
- 正确处理竞争请求，并使用延迟的 `rejected` 筛选验证。
- 实时申领旅程。
- 轮询生命周期。
- 状态变化反馈。
- 错误处理。
- 从不可用或过期 API 请求中恢复。
- 用户体验。
- 响应式行为。
- 无障碍。
- 合理使用框架。
- 可维护的前端代码。

评分不会考虑对所提供后端、数据库或端点契约的任何修改。只能使用本任务列出的面向用户操作。

## 分值分配

| WSOS 部分 | 描述 | 分值 |
|---:|---|---:|
| 1 | 工作组织与自我管理 | 4 |
| 2 | 沟通与人际技能 | 3 |
| 3 | 设计实现 | 8.5 |
| 4 | 前端开发 | 17.5 |
| 5 | 后端开发 | 0 |
|  | **总计** | **33** |

